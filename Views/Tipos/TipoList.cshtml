@model List<TipoModel>

<h1>Tipos de Productos</h1>

<a class = "btn btn-primary" asp-action="TipoAdd" asp-controller="Tipos">Agregar Tipo</a>

<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripcion</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var m in Model)
        {
            <tr>
                <td>@m.Nombre</td>
                <td>@m.Descripcion</td>
                <td>
                    <a class="btn btn-secondary" 
                    asp-action="TipoEdit" 
                    asp-controller="Tipos" 
                    asp-route-id="@m.Id">Editar</a>

                    <a class="btn btn-danger" 
                    asp-action="TipoDelete" 
                    asp-controller="Tipos" 
                    asp-route-id="@m.Id">Borrar</a>
                </td>
            </tr>
        }
    </tbody>
</table>